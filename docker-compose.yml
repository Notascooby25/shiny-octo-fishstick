version: "3.9"

services:
  wellness_app:
    build: .
    container_name: wellness_app
    env_file:
      - .env
    ports:
      - "8082:8000"
    volumes:
      - ./app:/app/app
      - wellness_db_data:/app/instance
    restart: unless-stopped

volumes:
  wellness_db_data:
